export enum ALLOWED_SORT_FIELDS {
  TICKER = "ticker",
  TYPE = "type",
  QUANTITY = "quantity",
  AVERAGE_PRICE_CENTS = "averagePriceCents",
  CURRENT_VALUE_CENTS = "currentValueCents",
  RESULT_CENTS = "resultCents",
  INSTITUTION = "institution",
  RETURN_PERCENTAGE = "returnPercentage",
}
