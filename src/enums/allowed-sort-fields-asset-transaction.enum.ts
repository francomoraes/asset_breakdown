export enum ALLOWED_SORT_FIELDS_ASSET_TRANSACTION {
  DATE = "date",
  TYPE = "type",
  QUANTITY = "quantity",
  UNIT_PRICE_CENTS = "unitPriceCents",
  TOTAL_AMOUNT_CENTS = "totalAmountCents",
  FEES_CENTS = "feesCents",
}
